# Per-project npm cache — keeps global cache clean
cache=./.npm-cache

# Save exact versions in package-lock
save-exact=true

# No fund messages
fund=false

# No audit on install (run manually when needed)
audit=false
