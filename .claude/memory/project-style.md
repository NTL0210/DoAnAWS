Project Preferences

Frontend

* React
* Next.js (Pages Router)
* JavaScript
* Context API
* TailwindCSS
* Framer Motion

Backend

* Node.js
* Express
* TypeScript (tsx + tsc)

Authentication

* Cognito JWT (production)
* JWT mock (development, localStorage-based)
* Role-Based Access Control (ADMIN / MANAGER / EMPLOYEE)

Architecture

* Reuse existing modules
* Preserve current UI
* Preserve current architecture

Performance

* Reduce cache size
* Reduce bundle size
* Reduce unnecessary dependencies

Voice

* Discord-like behavior
* Web Audio API noise suppression
* DTLN AI noise suppression worklet

Code Style

* Minimal
* Maintainable
* Backward compatible

Caching

* .next cache grows during dev — run clean script periodically
* npm cache global can exceed 2GB — clean with npm cache clean --force
* Prefer npm ci over npm install for reproducible builds
